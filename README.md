##  A cpp lib template that uses cmake, vcpkg, ninja and doctest
basd on https://github.com/lukka/CppCMakeVcpkgTemplate

### testing
```shell
# 1: Configure
cmake --preset=test
# 2: build
cmake --build --preset=tes
# 3: run test runner
./build/test/tester
```
